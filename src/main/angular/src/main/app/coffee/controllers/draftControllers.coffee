# Define the Application Controllers Module
draftControllers = angular.module 'draftControllers', []

# Define the GreetingListController Controller
draftControllers.controller 'numberFiltersController', ['$scope'
($scope) ->
]
